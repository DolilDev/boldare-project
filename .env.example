# Klucz API Anthropic — używany WYŁĄCZNIE po stronie serwera (route handler /api/chat).
# Pobierz go z https://console.anthropic.com/ i wklej do lokalnego pliku .env.local
# (a na Vercel ustaw jako zmienną środowiskową projektu).
# NIGDY nie commituj prawdziwego klucza — pliki .env* są w .gitignore.
ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
